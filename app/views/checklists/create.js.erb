<% if @error.present? %>
alert("<%= @error %>");
<%else%>
$('.survey-checklist').find('tbody').append("<%= escape_javascript(render 'checklist', checklist: @checklist).html_safe %>");
$('#checklist-modal').modal('hide');
<%end%>